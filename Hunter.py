class Hunter:
    name

    def __init__(self, playerName):
        caughtPrey(self)
        name = playerName
        while (gameOver == false):
            if (GPS.catchPrey()):
            # tell the game class
            if (GPS.checkHPinArea)
        # tell the player class a prey has been sighted

    


                