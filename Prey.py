class Prey:
    name
    def __init__(self, playerName):
        checkHPInArea()



    def checkHPInArea(self):
        while ():  # keeps checking if a hunter is in the vicinity
            # calls method from GPS class
            if (GPS.checkHunter(self.name)):
                notifications.hunterIsInArea()
